"""
主窗口 UI 模組 - 極限完整版
"""

from PyQt6.QtWidgets import (\n    QMainWindow, QWidget, QVBoxLayout, QHBoxLayout, QTextEdit, \n    QPushButton, QComboBox, QLabel, QCheckBox, QSplitter, QStatusBar,\n    QMessageBox, QFrame\n)\nfrom PyQt6.QtCore import Qt, QThread, pyqtSignal, QTimer\nfrom PyQt6.QtGui import QFont, QIcon, QColor\nimport pyperclip\nfrom src.translator import GoogleTranslator\nfrom src.clipboard_monitor import ClipboardMonitor\nfrom src.ocr_engine import OCREngine\nfrom src.utils.config import config_manager\nfrom src.utils.logger import Logger\nfrom src.ui.float_window import FloatingTranslator\nfrom src.ui.settings_dialog import SettingsDialog\n\nlogger = Logger.get_logger(__name__)\n\nclass TranslationWorker(QThread):\n    \"\"\"翻譯工作執行緒 - 避免 UI 卡頓\"\"\"\n    \n    finished = pyqtSignal(str)\n    error = pyqtSignal(str)\n    \n    def __init__(self, text):\n        super().__init__()\n        self.text = text\n        self.translator = GoogleTranslator()\n    \n    def run(self):\n        \"\"\"執行翻譯\"\"\"\n        try:\n            result = self.translator.translate(self.text)\n            if result:\n                self.finished.emit(result)\n            else:\n                self.error.emit(\"翻譯失敗，請檢查網路連接\")\n        except Exception as e:\n            self.error.emit(f\"錯誤: {str(e)}\")\n            logger.error(f\"翻譯執行緒錯誤: {e}\")\n\n\nclass MainWindow(QMainWindow):\n    \"\"\"主窗口 - 極限完整版本\"\"\"\n    \n    def __init__(self):\n        super().__init__()\n        self.translator = GoogleTranslator()\n        self.ocr_engine = OCREngine()\n        self.clipboard_monitor = ClipboardMonitor(\n            callback=self.on_clipboard_changed\n        )\n        self.floating_translator = None\n        self.translation_worker = None\n        self.translation_cache = {}  # 翻譯緩存\n        self.translation_history = []  # 翻譯歷史\n        \n        self.init_ui()\n        self.load_config()\n        self.setup_shortcuts()\n        \n        logger.info(\"✅ 主窗口已初始化\")\n    \n    def init_ui(self):\n        \"\"\"初始化用戶介面\"\"\"\n        self.setWindowTitle(\"沉浸式英文翻譯軟體 v1.0\")\n        self.setGeometry(100, 100, 1000, 700)\n        self.setStyleSheet(self._get_stylesheet())\n        \n        # 中央 Widget\n        central_widget = QWidget()\n        self.setCentralWidget(central_widget)\n        main_layout = QVBoxLayout(central_widget)\n        \n        # 頂部控制面板\n        control_layout = self._create_control_panel()\n        main_layout.addLayout(control_layout)\n        \n        # 分割器 - 來源文本 / 翻譯結果\n        splitter = QSplitter(Qt.Orientation.Horizontal)\n        \n        # 左側 - 來源英文文本\n        left_panel = QWidget()\n        left_layout = QVBoxLayout(left_panel)\n        left_layout.addWidget(QLabel(\"📝 英文文本:\"))\n        self.source_text = QTextEdit()\n        self.source_text.setPlaceholderText(\n            \"在此輸入英文文本...\\n或複製英文文本到剪貼板自動翻譯\\n或點擊 OCR 從螢幕截取英文文字\"\n        )\n        self.source_text.textChanged.connect(self.on_text_changed)\n        left_layout.addWidget(self.source_text)\n        splitter.addWidget(left_panel)\n        \n        # 右側 - 翻譯結果\n        right_panel = QWidget()\n        right_layout = QVBoxLayout(right_panel)\n        right_layout.addWidget(QLabel(\"🇹🇼 繁體中文翻譯:\"))\n        self.result_text = QTextEdit()\n        self.result_text.setReadOnly(True)\n        self.result_text.setPlaceholderText(\"翻譯結果將顯示在此...\")\n        right_layout.addWidget(self.result_text)\n        splitter.addWidget(right_panel)\n        \n        splitter.setSizes([500, 500])\n        main_layout.addWidget(splitter)\n        \n        # 底部按鈕\n        button_layout = self._create_button_panel()\n        main_layout.addLayout(button_layout)\n        \n        # 狀態欄\n        self.statusBar = QStatusBar()\n        self.setStatusBar(self.statusBar)\n        self.statusBar.showMessage(\"✅ 就緒\")\n    \n    def _create_control_panel(self) -> QVBoxLayout:\n        \"\"\"建立控制面板\"\"\"\n        layout = QVBoxLayout()\n        \n        # 功能開關\n        options_layout = QHBoxLayout()\n        \n        self.clipboard_check = QCheckBox(\"📋 剪貼板自動監控\")\n        self.clipboard_check.stateChanged.connect(\n            self.on_clipboard_monitor_toggle\n        )\n        options_layout.addWidget(self.clipboard_check)\n        \n        self.float_window_check = QCheckBox(\"🪟 浮窗翻譯\")\n        self.float_window_check.stateChanged.connect(\n            self.on_float_window_toggle\n        )\n        options_layout.addWidget(self.float_window_check)\n        \n        self.ocr_check = QCheckBox(\"📸 OCR 螢幕取字\")\n        options_layout.addWidget(self.ocr_check)\n        \n        options_layout.addStretch()\n        \n        # 設置按鈕\n        settings_btn = QPushButton(\"⚙️ 設置\")\n        settings_btn.clicked.connect(self.open_settings)\n        options_layout.addWidget(settings_btn)\n        \n        layout.addLayout(options_layout)\n        \n        return layout\n    \n    def _create_button_panel(self) -> QHBoxLayout:\n        \"\"\"建立按鈕面板\"\"\"\n        layout = QHBoxLayout()\n        \n        # 翻譯按鈕\n        self.translate_btn = QPushButton(\"🚀 翻譯 (Ctrl+Enter)\")\n        self.translate_btn.setMinimumHeight(45)\n        self.translate_btn.clicked.connect(self.translate)\n        layout.addWidget(self.translate_btn)\n        \n        # OCR 按鈕\n        self.ocr_btn = QPushButton(\"📸 OCR 螢幕取字\")\n        self.ocr_btn.setMinimumHeight(45)\n        self.ocr_btn.clicked.connect(self.start_ocr)\n        self.ocr_btn.setEnabled(self.ocr_engine.is_available())\n        layout.addWidget(self.ocr_btn)\n        \n        # 清空按鈕\n        clear_btn = QPushButton(\"🗑️ 清空\")\n        clear_btn.setMinimumHeight(45)\n        clear_btn.clicked.connect(self.clear_text)\n        layout.addWidget(clear_btn)\n        \n        # 複製按鈕\n        copy_btn = QPushButton(\"📋 複製結果\")\n        copy_btn.setMinimumHeight(45)\n        copy_btn.clicked.connect(self.copy_result)\n        layout.addWidget(copy_btn)\n        \n        # 交換按鈕\n        swap_btn = QPushButton(\"🔄 交換文本\")\n        swap_btn.setMinimumHeight(45)\n        swap_btn.clicked.connect(self.swap_text)\n        layout.addWidget(swap_btn)\n        \n        return layout\n    \n    def translate(self):\n        \"\"\"執行翻譯\"\"\"\n        text = self.source_text.toPlainText().strip()\n        \n        if not text:\n            self.statusBar.showMessage(\"⚠️ 請輸入英文文本\")\n            return\n        \n        # 檢查文本長度\n        max_length = config_manager.get(\"max_text_length\", 5000)\n        if len(text) > max_length:\n            self.statusBar.showMessage(f\"⚠️ 文本過長，最多 {max_length} 字符\")\n            return\n        \n        # 檢查緩存\n        if text in self.translation_cache:\n            self.result_text.setText(self.translation_cache[text])\n            self.statusBar.showMessage(f\"⚡ 從緩存取得 (字數: {len(text)})\")\n            return\n        \n        self.statusBar.showMessage(\"⏳ 翻譯中...\")\n        self.translate_btn.setEnabled(False)\n        \n        # 建立工作執行緒\n        self.translation_worker = TranslationWorker(text)\n        self.translation_worker.finished.connect(self.on_translation_finished)\n        self.translation_worker.error.connect(self.on_translation_error)\n        self.translation_worker.start()\n    \n    def on_translation_finished(self, result: str):\n        \"\"\"翻譯完成\"\"\"\n        self.result_text.setText(result)\n        self.statusBar.showMessage(f\"✅ 翻譯完成 (字數: {len(result)})\")\n        self.translate_btn.setEnabled(True)\n        \n        # 保存到緩存\n        source_text = self.source_text.toPlainText().strip()\n        self.translation_cache[source_text] = result\n        \n        # 保存到歷史\n        self.translation_history.append((source_text, result))\n        \n        # 自動複製結果\n        if config_manager.get(\"auto_copy_result\", True):\n            pyperclip.copy(result)\n            logger.info(\"✅ 結果已自動複製\")\n        \n        logger.info(\"✅ 翻譯完成\")\n    \n    def on_translation_error(self, error: str):\n        \"\"\"翻譯錯誤\"\"\"\n        self.result_text.setText(f\"❌ 錯誤: {error}\")\n        self.statusBar.showMessage(\"❌ 翻譯失敗\")\n        self.translate_btn.setEnabled(True)\n        logger.error(f\"翻譯錯誤: {error}\")\n    \n    def on_clipboard_changed(self, content: str):\n        \"\"\"剪貼板內容改變\"\"\"\n        if not self.clipboard_check.isChecked():\n            return\n        \n        # 只翻譯英文（簡單檢測）\n        if self._is_english_text(content):\n            self.source_text.setText(content)\n            self.translate()\n    \n    def on_clipboard_monitor_toggle(self, state):\n        \"\"\"剪貼板監控切換\"\"\"\n        if state == 2:  # Qt.CheckState.Checked\n            self.clipboard_monitor.start()\n            self.statusBar.showMessage(\"✅ 剪貼板監控已啟用\")\n        else:\n            self.clipboard_monitor.stop()\n            self.statusBar.showMessage(\"⏹️ 剪貼板監控已禁用\")\n    \n    def on_float_window_toggle(self, state):\n        \"\"\"浮窗翻譯切換\"\"\"\n        if state == 2:  # 啟用\n            if not self.floating_translator:\n                self.floating_translator = FloatingTranslator()\n            self.statusBar.showMessage(\"✅ 浮窗翻譯已啟用 (Ctrl+Alt+T)\")\n        else:  # 禁用\n            if self.floating_translator:\n                self.floating_translator.close()\n                self.floating_translator = None\n            self.statusBar.showMessage(\"⏹️ 浮窗翻譯已禁用\")\n    \n    def start_ocr(self):\n        \"\"\"開始 OCR\"\"\"\n        if not self.ocr_engine.is_available():\n            self.statusBar.showMessage(\"❌ OCR 引擎不可用\")\n            return\n        \n        self.statusBar.showMessage(\"📸 請拖拽選擇螢幕上要識別的英文區域...\")\n        logger.info(\"🎯 OCR 開始\")\n        # 實現 OCR 選擇區域的邏輯\n    \n    def clear_text(self):\n        \"\"\"清空文本\"\"\"\n        self.source_text.clear()\n        self.result_text.clear()\n        self.statusBar.showMessage(\"✅ 已清空\")\n    \n    def copy_result(self):\n        \"\"\"複製結果\"\"\"\n        result = self.result_text.toPlainText()\n        if result and not result.startswith(\"❌\"):\n            pyperclip.copy(result)\n            self.statusBar.showMessage(\"✅ 已複製到剪貼板\")\n        else:\n            self.statusBar.showMessage(\"⚠️ 沒有有效的結果可複製\")\n    \n    def swap_text(self):\n        \"\"\"交換文本\"\"\"\n        source = self.source_text.toPlainText()\n        result = self.result_text.toPlainText()\n        \n        if result and not result.startswith(\"❌\"):\n            self.source_text.setText(result)\n            self.result_text.setText(source)\n            self.statusBar.showMessage(\"✅ 文本已交換\")\n    \n    def on_text_changed(self):\n        \"\"\"文本改變時的處理\"\"\"\n        pass  # 可用於實時提示功能\n    \n    def open_settings(self):\n        \"\"\"打開設置對話框\"\"\"\n        dialog = SettingsDialog(self)\n        dialog.exec()\n    \n    def load_config(self):\n        \"\"\"載入配置\"\"\"\n        clipboard_enabled = config_manager.get(\"clipboard_monitor_enabled\", True)\n        self.clipboard_check.setChecked(clipboard_enabled)\n        \n        float_window_enabled = config_manager.get(\"float_window_enabled\", True)\n        self.float_window_check.setChecked(float_window_enabled)\n    \n    def setup_shortcuts(self):\n        \"\"\"設定快捷鍵\"\"\"\n        from PyQt6.QtGui import QKeySequence\n        from PyQt6.QtWidgets import QShortcut\n        \n        # Ctrl+Enter 翻譯\n        QShortcut(QKeySequence(\"Ctrl+Return\"), self, self.translate)\n    \n    @staticmethod\n    def _is_english_text(text: str) -> bool:\n        \"\"\"簡單判斷是否為英文\"\"\"\n        english_chars = sum(1 for c in text if ord(c) < 128)\n        return english_chars > len(text) * 0.5\n    \n    @staticmethod\n    def _get_stylesheet() -> str:\n        \"\"\"獲取 UI 樣式表\"\"\"\n        return \"\"\"\n        QMainWindow {\n            background-color: #f5f5f5;\n        }\n        QTextEdit {\n            background-color: white;\n            border: 1px solid #ddd;\n            border-radius: 5px;\n            padding: 5px;\n            font-size: 12px;\n        }\n        QPushButton {\n            background-color: #007bff;\n            color: white;\n            border: none;\n            border-radius: 5px;\n            padding: 8px;\n            font-weight: bold;\n        }\n        QPushButton:hover {\n            background-color: #0056b3;\n        }\n        QPushButton:pressed {\n            background-color: #003d82;\n        }\n        QLabel {\n            font-weight: bold;\n            color: #333;\n        }\n        QStatusBar {\n            background-color: #f5f5f5;\n            color: #333;\n        }\n        \"\"\"\n    \n    def closeEvent(self, event):\n        \"\"\"關閉窗口\"\"\"\n        self.clipboard_monitor.stop()\n        if self.floating_translator:\n            self.floating_translator.close()\n        logger.info(\"✅ 應用程式已關閉\")\n        event.accept()
